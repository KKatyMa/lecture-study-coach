# Groq (server-side only — never use NEXT_PUBLIC_ for this key)
GROQ_API_KEY=

# Optional: local Ollama overrides when preset is "ollama"
# OLLAMA_BASE_URL=http://localhost:11434/v1
# OLLAMA_MODEL=qwen2.5:14b
